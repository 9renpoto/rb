{
  "name": "myapp",
  "dependencies": {},
  "devDependencies": {
    "@prettier/plugin-ruby": "0.17.0",
    "husky": "4.0.4",
    "lint-staged": "9.5.0",
    "prettier": "1.19.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,md,yml}": [
      "prettier --write",
      "git add"
    ],
    "{app,test,db,lib,spec}/**/*.{rb,rake}": [
      "prettier --write",
      "bundle exec rubocop -a",
      "git add"
    ]
  },
  "private": true,
  "scripts": {
    "test": ""
  }
}
