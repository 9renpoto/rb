{
  "name": "myapp",
  "dependencies": {},
  "devDependencies": {
    "@prettier/plugin-ruby": "0.12.1",
    "husky": "2.1.0",
    "lint-staged": "8.1.5",
    "prettier": "1.17.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "ignore": [
      "db/migrate/*.rb",
      "db/schema.rb"
    ],
    "linters": {
      "*.{js,md,yml}": [
        "prettier --write",
        "git add"
      ],
      "{app,test,db,lib,spec}/**/*.{rb,rake}": [
        "prettier --write",
        "bundle exec rubocop -a",
        "git add"
      ]
    }
  },
  "private": true,
  "scripts": {
    "test": ""
  }
}
