{
  "name": "myapp",
  "dependencies": {},
  "devDependencies": {
    "@prettier/plugin-ruby": "0.21.0",
    "husky": "4.3.0",
    "lint-staged": "10.5.2",
    "prettier": "2.2.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,md,yml}": [
      "prettier --write",
      "git add"
    ],
    "{app,test,db,lib,spec}/**/*.{rb,rake}": [
      "prettier --write",
      "bundle exec rubocop -a",
      "git add"
    ]
  },
  "private": true,
  "scripts": {
    "test": ""
  }
}
