{
  "name": "myapp",
  "dependencies": {},
  "devDependencies": {
    "@prettier/plugin-ruby": "0.17.0",
    "husky": "4.2.3",
    "lint-staged": "10.0.7",
    "prettier": "1.19.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,md,yml}": [
      "prettier --write"
    ],
    "{app,test,db,lib,spec}/**/*.{rb,rake}": [
      "prettier --write",
      "bundle exec rubocop -a"
    ]
  },
  "private": true,
  "scripts": {
    "test": ""
  }
}
